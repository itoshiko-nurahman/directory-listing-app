<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Places - BestPoint</title>
</head>
<body>
    <h1>Update Places</h1>
    <form action="/places/<%= place._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title :</label>
            <input type="text" id="title" name="place[title]" required value="<%= place.title %>">
        </div>
        <div>
            <label for="location">Location :</label>
            <input type="text" id="location" name="place[location]" required value="<%= place.location %>">
        </div>
        <div>
            <label for="description">Description :</label>
            <input type="text" id="description" name="place[description]" required value="<%= place.description %>">
        </div>
        <div>
            <label for="price">Price :</label>
            <input type="text" id="price" name="place[price]" required value="<%= place.price %>">
        </div>

        <button type="submit">Update</button>
    </form>

</body>
</html>